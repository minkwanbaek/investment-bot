[BACKTEST_SUMMARY]
run_id:
iteration:
timestamp:

strategy_version:
baseline_version:

period_start:
period_end:
market_scope: upbit-auth-all
timeframes: 5m,15m

net_profit_pct:
delta_net_profit_pct:

max_drawdown_pct:
delta_max_drawdown_pct:

profit_factor:
delta_profit_factor:

total_trades:
avg_holding_min:

fees_applied: yes
slippage_applied: yes
liquidity_filter_applied: yes
min_order_constraint_applied: yes

top_loss_context:
largest_problem:
tested_modification:

verdict: promising | unclear | reject
[/BACKTEST_SUMMARY]
